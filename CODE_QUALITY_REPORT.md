# Code Quality Report — Crab Project

Generated for commit `0c9cc5538538af4daf0d04610348c8c85639cd22` plus uncommitted worktree changes (commit date: 2026-08-13).

This report is generated by `scripts/gen_code_quality_report.sh`.

## Tool Versions

- rustc 1.93.0 (254b59607 2026-01-19)
- cargo 1.93.0 (083ac5135 2025-12-15)
- Python 3.14.3
- cargo-llvm-cov 0.6.21 (optional)
- (missing: cargo-audit)
- node v25.6.1
- (missing: scc)
- jscpd 4.0.5 (optional)

## Enforced Quality Gates

All required gates are defined in `Makefile` and enforced by CI in `.github/workflows/quality.yml`:

- `fmt`: `cargo fmt --all -- --check`
- `clippy`: repository policy with rustc warnings and correctness/suspicious/perf denied;
  style/complexity force-warned
- `tests`: full workspace tests in the normal build configuration

Coverage, duplication, public-API, and workflow-tool checks are available as opt-in diagnostics.

Canonical agent loop:

- `make doctor`
- `make check`
- `make quality`

The authoritative gate writes a fingerprinted `quality/status.json` and rejects required skips.

## Size (SLOC)

(missing: install scc)

## Runtime + Density Metrics

- Workspace crates: 8
- Production LOC (production Rust, excluding `src/test_support.rs`): 53408
- Workspace test attributes (`#[test]`, `#[tokio::test]`): 1101
- Tests/KLOC: 20.61
- `cfg(not(coverage))` occurrences: 17
- Quality-fix-like commits (30d): 0
- Quality-fix-like commits (90d): 0

## Baseline Snapshot

No recorded baseline yet. Run `make quality-baseline` to capture one.

## Baseline Trend

No baseline trend yet. Capture at least two baselines with `make quality-baseline`.

## Optional Coverage Hotspots (from `coverage/lcov.info` when present)

Coverage is a report-only diagnostic with no required percentage floor.

Aggregate production coverage excludes every `src/test_support.rs` through the shared export
policy. Hotspots below use LCOV `LF`/`LH` totals; `DA:0` rows supply locations only.

Coverage hotspot view unavailable: run `make coverage` first to generate `coverage/lcov.info`.

## Tests

- Test attributes in workspace source: 1101

## Dependency Health (cargo-audit)

    --- cargo-audit output ---
(cargo-audit not installed)
    --- end ---
